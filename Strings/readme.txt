😎 Strings:

1. Reverse a string.
2. Check if a string is a palindrome.
3. Check if two strings are anagrams of each other.
4. Compress a string by counting consecutive characters.
5. Capitalize the first letter of each word in a sentence.
6. Determine if one string is a rotation of another.
7. Generate all permutations of a string.
8. Truncate a string to a specified length and add ellipsis if necessary.
9. Validate if a string contains only valid characters (e.g., alphanumeric).
10. Count the number of occurrences of a substring in a larger string.